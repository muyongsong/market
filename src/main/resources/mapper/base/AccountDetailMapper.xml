<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.base.mapper.AccountDetailMapper">
    <resultMap type="com.example.demo.base.entity.AccountDetail" id="accountMap">
        <id column="id" property="id"/>
        <result column="telephone_number" property="telephoneNumber"/>
        <result column="email" property="email"/>
        <result column="personal_signature" property="personalSignature"/>
    </resultMap>

    <!-- 创建用户 -->
    <insert id="addAccountDetail" parameterType="com.example.demo.base.entity.AccountDetail">

        insert into accountDetailRecord (id, user_name, password)
        values (#{id,jdbcType=VARCHAR}, #{userName,jdbcType=VARCHAR}, #{userPassword,jdbcType=VARCHAR})

    </insert>

</mapper>